# Melodialect 仕様書

**作曲家の「音楽的方言」をルールベースで再現する楽曲ジェネレーター**

- バージョン: 0.8.0
- 作成日: 2026-07-18
- 参考: [Claude Fable 5 によるビートルズ風楽曲ジェネレーターの記事(TechnoEdge)](https://www.techno-edge.net/article/2026/07/13/5285.html)

---

## 1. コンセプト

機械学習モデルを使わず、**音楽理論に基づく明示的なルール(=作曲家ごとの「方言」)** をコードとして実装し、それらを組み合わせて楽曲(コード進行+メロディ+伴奏)を生成するブラウザアプリ。

- 学習データ不要・著作物の複製なし。生成されるのは「技法・傾向」を模した完全オリジナル曲
- ルールがすべてコードで可視化されているため、「なぜこの音になったか」を説明できる(生成根拠の解説機能)
- 生成結果は WAV / MIDI(SMF) で書き出し、Logic Pro 等の DAW や Suno 等の外部サービスに持ち込める

## 2. 用語

| 用語 | 意味 |
|---|---|
| ダイアレクト(Dialect) | 作曲家 1 人分の作曲スタイル定義。コード進行・メロディ輪郭・リズム傾向のルールセット |
| セクション | Verse / Chorus / Bridge / Intro / Outro などの楽曲構成単位 |
| ソング | セクションの並び+キー+テンポ+ダイアレクト割り当ての全体 |

## 3. 対象ユーザーとユースケース

1. **DTM ユーザー**: 作曲の叩き台(コード進行+メロディ)を生成し、MIDI で DAW に取り込んで仕上げる
2. **音楽理論の学習者**: 「モーダル・インターチェンジを使うとどう響くか」を、ルールの ON/OFF で聴き比べて学ぶ
3. **AI 歌声サービス利用者**: 生成した曲+仮歌詞を Suno 等に渡してボーカル入りデモを作る

## 4. 機能要件

### 4.1 ダイアレクト(スタイルプリセット)

14 種類 (D1〜D14) を実装する。各ダイアレクトは以下の 8 層で定義する:

1. **コード進行** — 語彙+マルコフ遷移に加え、3〜4 コードの定型句(イディオム)、終止形の好み(全終止/変格終止/偽終止/モーダル終止)、ハーモニックリズム(2 小節 1 コード〜1 小節 2 コード)
2. **リズム語彙** — ダイアレクト別の重み付きリズムテンプレート(シンコペーション・休符を含む)とアウフタクト(先取音)の傾向
3. **モチーフ** — 冒頭フレーズのリズムと輪郭を記憶し、以降のフレーズで反復・移調反復(シークエンス)する確率
4. **メロディ輪郭** — 跳躍確率・跳躍幅・跳躍後バイアス・同音連打・逆ペダル
5. **非和声音とセクション対比** — 倚音・掛留・半音階経過音の確率、セクション別の音域シフトとサビの音数バイアス
6. **転調傾向** — ブリッジ等での転調確率と移調量
7. **音集合と旋律輪郭** — 長音階/ミクソリディアン/自然短音階に加え、和声的短音階・五音音階・ブルース音階と、弧形/コール&レスポンス/下降/交差/声部連結の実働アルゴリズム
8. **グルーヴと推奨伴奏** — 3-3-2 等のアクセント、コード先取り、スウィング、ダイアレクト別のピアノ/ギター/ドラム初期値

#### D1: Modal(モーダル進行+語りの同音連打)
- コード: モーダル・インターチェンジ多用(♭VII, ♭VI, iv)。♭VI→♭VII→I の定型句、♭VII→I のモーダル終止。V7 の機能和声を抑え、2 小節 1 コードも使うゆったりしたハーモニックリズム
- メロディ: ミクソリディアン音集合による反復音・狭い音域・同音連打。休符を挟む語りのリズム。短いモチーフの執拗な反復
- 構成: フレーズ長の変則化(7 小節など)。変則化は1セクションにつき1小節だけとし、意図しない6小節化を防ぐ。サビでも音域がほぼ上がらないフラットなダイナミクス

#### D2: Chromatic(半音階クリシェ+歌心のモチーフ反復)
- コード: 半音階クリシェ(C → C/B → C/B♭ → F/A)。I→vi→ii7→V7 に加え、III7→vi、IV→iv→I の色彩変化を定型句として使う
- メロディ: 順次進行主体で歌いやすく、跳ねる付点リズムとアウフタクト。モチーフ反復 (0.6) と倚音
- ベース: ルートのオクターブを近接配置し、コードトーンから次のルートへ半音または順次進行で接近する専用の対旋律パターン。サビは音域 +3 半音・音数増
- 構成: 8小節を基本に、低確率で1小節だけ短縮した7小節フレーズを作る

#### D3: Pedal(逆ペダル+掛留のドローン)
- コード: 逆ペダルポイント(メロディ固定・コードのみ変化)。I→♭VII→IVadd9→I、Isus4→I、変格終止中心
- メロディ: ミクソリディアンのペダル音を軸に、弱拍で隣接音へ離れて強拍・和声境界で戻る。掛留を明示的に解決し、長い音価のドローンリズムを保つ
- 伴奏: 低声部は調のルートと5度だけを交互に保つドローン、上声はギターアルペジオで sus4 / add9 の内声変化を聴かせる。II7 は経過的に使用

#### D4: Twilight(哀愁進行+大跳躍シンコペーション)
- メロディ: **フレーズ頭で大きく上方跳躍し、その後なだらかに下降**する輪郭
  - 跳躍確率: 通常フレーズ 28%、サビ頭 95%
  - 跳躍幅: 7〜10 半音(完全5度〜短7度)
  - 跳躍直後は下降方向にバイアス。倚音 (0.22) の多い哀愁の節回し
- コード: メジャー7th を含むソフトな響き。IV△7→III7→vi の哀愁定型句 (0.45)、サビは 1 小節 2 コードに細分
- リズム: シンコペーションと裏拍入り、アウフタクト多用 (0.5)。サビは音域 +4 半音・音数増
- 転調: ブリッジで下属調/属調へ転調しやすい (0.6)
- テンポ/性格: スローバラード寄りのデフォルト値

#### D5: Angular(七の和音+方向反転する跳躍ポップ)
- コード: △7/m7 主体の洗練和声、iii7→vi7→ii7→V7 と I△7→II7→V7→iii7 の定型句、ii7→I△7 のソフトな終止。1 小節 2 コードを通常 0.55、サビ 0.7 で選ぶ
- メロディ: 2〜3度の移動と方向反転を組み合わせる専用輪郭に、5〜11半音の跳躍 (0.34/サビ頭 0.78) と倚音 (0.24) を重ねる。跳躍後も一方向へ固定せず、角張った動きを継続する
- リズム/構成: シンコペーションとアウフタクト (0.48)、サビ +4 半音。変則フレーズ確率 0.42、ブリッジで全音上へ転調しやすい

#### D6: Orchestral(下降転回ベース+浮遊和声のポップ)
- コード: Verse系では I△7→V7/3→vi7→iii7/3 の上声を保ちながらベースを 1–7–6–5、Chorusでは vi7→iii7/3→IV△7→I△7/3 と 6–5–4–3 に下降させる4小節技法を高確率 (0.82) で適用。♭VI△7→♭VII→I△7 のリフト、変格終止と偽終止傾向も併用
- メロディ/伴奏: 音域中央へ戻る浮遊輪郭、同音保持、掛留 (0.28) とゆったりした音価を使う。低音は転回音を起点にコード内声と次コードへの接近音を4分音符でつなぐ
- 転調: ブリッジで全音上/下属調へ (0.55)。サビ +3 半音のレジスタ

#### D7: Bossa(ルートレス和音+2小節シンコペーション)
- 伴奏: 3rd/7th/9th 中心の疎なピアノと、上声を滑らかにつなぐナイロンギターを別々の2小節型で配置。低音は1・3拍のルート/5度とコード直前の8分先取り、打楽器はストレート8分と2小節のクロススティック
- コード: I△9 / ii9 / V9 を軸に、VI7→ii9→V9、I7→IV△9→iv7 の定型句を高確率で挿入。1小節2コードを主体とする
- メロディ: 休符から入る短いフレーズと裏拍、狭い音域の順次進行を主体にし、同音反復と大きな跳躍は控えめ。フレーズ末の先取音を使う
- 性格: 124 BPMのストレート8分。サビでも密度と音域の対比を抑え、ブリッジの転調も控えめにする

#### D8: Ostinato(機械的リフレイン+ミニマル短調)
- コード: 短調 (デフォルト A minor)。i / VI / VII 中心のミニマル語彙、2 小節 1 コード (0.4)、VII→i のモーダル終止
- メロディ: 均質な 8 分連打 (同音連打 0.5)、モチーフ反復 0.85 のシーケンサ的ループ。跳躍・非和声音・アウフタクトなし
- 構成: 変則なしの機械的な規則性。セクション対比もフラット

#### D9: Serene(揺れる△7+静謐な 3 拍子)
- 拍子: **デフォルト 3/4** (ダイアレクト別デフォルト拍子)。テンポ 66 の静かなワルツ
- コード: I△7↔IV△7 の揺れ (定型句 0.45)、2 小節 1 コード (0.35)、ii7→I△7 / IV△7→I△7 の柔らかい終止のみ (V7 をほぼ使わない)
- メロディ: 1 拍目を休符にして 2 拍目から入る疎らなリズム語彙、掛留 (0.18)、モチーフ反復 0.7。サビとの対比はほぼなし (+1 半音)

#### D10: Flow(複合拍子+循環アルペジオ)
- 拍子/伴奏: 6/8 の付点4分パルス。ピアノとギターのアルペジオ、複合拍子ドラムを推奨伴奏として自動選択
- コード: I→V7→vi→IV と I→♭VII→IV→I。Intro は 4 小節、Chorus は 8 小節のセクション規則
- メロディ: メジャー五音音階、前半上昇・後半下降の弧形輪郭、アウフタクトと高いモチーフ反復率

#### D11: Blue(12小節循環+ブルーノート)
- コード: I7 / IV7 / V7 による固定 12 小節ブルース。最終セクションの終止形と途中セクションのターンアラウンドを保持
- メロディ: ♭3・♭5・♭7 を含むブルース音階。上行する呼びかけと下降する応答を交互に生成
- グルーヴ: シャッフルドラム、スウィング、シンコペピアノ、ギターストラムを自動選択

#### D12: Lament(下降短調+導音カデンツ)
- コード: i→VII→VI→V7 のラメント・バス、ii°→V7→i、強い導音を持つ短調終止
- メロディ: 和声的短音階と下降輪郭。倚音・掛留・半音階経過音を多めに使用
- セクション: Intro/Outro を 4 小節にし、Chorus では i→VI→III→V7 の定型句を優先

#### D13: Interlock(3-3-2+交差リズム)
- グルーヴ: 小節内 0 / 1.5 / 3 拍の 3-3-2 アクセント、0.25 拍のコード先取り、専用ベース/ギター/ドラムパターン
- コード: 2 小節 1 コードを含む簡潔な短調循環。i→VI→III→VII と i→VII→VI→VII を優先
- メロディ: マイナー五音音階、短い上下動を噛み合わせる輪郭、同音反復と高いモチーフ再利用率

#### D14: Voicing(拡張和音+滑らかな声部連結)
- コード: △9 / m9 / 9 / add9 / sus2・sus4 / m7♭5 を使用。ii9→Vsus4→V9→I△9、IVsus2→IV△9→iv9→I△9 を優先し、サスペンションの解決を進行として聞かせる
- 伴奏: 9th和音では5度を省いた root / 3rd / 7th / 9th の4声を使用。候補となる全転回形を比較し、総移動量・トップノートの跳躍・音域を評価して前コードから最も滑らかな配置を選ぶ
- メロディ: 共通音と近接音へ戻る輪郭、同音保持 (0.24)、掛留 (0.28) と半音階経過音を使用

> ダイアレクトは JSON ベースの宣言的定義とし、後から追加・ユーザー編集できる設計にする(§6.2)。

### 4.2 楽曲生成

| 項目 | 内容 |
|---|---|
| キー | 12 キー × メジャー/マイナー。デフォルトはダイアレクトごとの推奨キー |
| テンポ | 40〜200 BPM。ダイアレクトごとのデフォルト値あり |
| 拍子 | 4/4 / 3/4 / 6/8(6/8 は複合拍子として 1 小節 = 付点4分 × 2 拍で扱う) |
| セクション構成 | Intro / Verse / Chorus / Bridge / Outro を並べて指定。プリセット(例: V-C-V-C-B-C)あり |
| セクション別ダイアレクト割り当て | 「Verse は Modal、Chorus は Chromatic」のような合作モードをサポート |
| シード | 乱数シード指定で同じ曲を再現可能 |
| エンディング | "final" (既定) = ダイアレクト別の終止カデンツ+終止和音を 1 小節保持するコーダ。"loop" = 最終セクションも半終止で終え、最後のメロディ音を曲頭の音へ寄せてシームレスにリピートできる継ぎ目を生成 (再生もループ) |

生成パイプライン(すべて決定的ルール+シード付き乱数):

1. **構成生成**: セクション列とフレーズ長(変則小節数を含む)を決定
2. **コード進行生成**: コードASTと機能和声グラフを使い、セクションの到達先と緊張曲線に向かう複数経路を探索。ダイアレクトの定型句、借用和音、セカンダリードミナント、ピボットコード、拡張・変化和音、スラッシュコードを適用
3. **メロディ生成**: コードトーン/テンションを土台に、輪郭ルール(跳躍確率・跳躍幅・下降バイアス)でピッチ列を生成し、リズムテンプレートに割り付け
4. **ベース生成**: セクションごとの役割と活動量を計画し、コード、メロディ、キックとの接続を評価してセクション全体の経路を選択
5. **伴奏生成**: `ArrangementPlan` に従ってピアノ、ギター、ドラムの参加、密度、音域、テクスチャ、フィル、ブレイクを決め、互換性のあるデータ駆動パターンへコードを展開
6. **候補評価と選択**: 制約違反を棄却し、和声、声部連結、旋律適合、ベース、伴奏、セクション対比を評価。指紋が近い候補を除き、上位群からシード付き重み選択
7. **仮歌詞生成**: 音節数に合わせたラ・ラ・ラ/日本語ダミー歌詞を割り当て(任意)

乱数はルートシードから `structure` / `harmony` / `melody` / `bass` / `piano` / `guitar` / `drums` / `humanize` の名前付きストリームへ派生させ、さらにセクション番号と候補番号で分離する。生成順序の変更やパート別再生成が対象外パートへ波及しない決定性を保つ。

通常のUI生成は内部3候補から1案を自動選択する。候補0を互換基準とし、代替候補はコード適合、声部連結、Chorus／Outroの密度範囲などのガードを満たし、測定上の改善がある場合だけ自動採用する。任意の「他の候補」は同じシードから最大4案を返す。候補数はエンジンAPIの引数でありUI仕様には固定しない。候補指紋は和声、旋律、ベース、伴奏の特徴を分離して保持し、UIでは数値評価ではなく現在案との差分タグへ変換する。

### 4.3 ベースと伴奏

- 楽器: ピアノ / アコースティックギター(アルペジオ・ストローク) / エレキベース / 簡易ドラム
- ベースは伴奏から独立した `BassProfile` を持ち、root / pedal / walking / ostinato / counterline の役割、活動量、音域、シンコペーション、休符、アプローチ、囲い込み、跳躍解決、フィルを記述する
- `ArrangementPlan` はセクション単位で楽器参加、密度、音域、テクスチャ、フィル、ブレイク、ピックアップを記述し、ピアノ主体 / ギター主体 / 交互配置 / アンサンブルの戦略を選択する。音域変更はコードのピッチクラスを変えないオクターブ単位に限定する
- 各楽器 4 パターン以上(例: ピアノ = ブロックコード、アルペジオ、8 分刻み、バラードパターン)をデータ駆動の互換プリセットとして維持する
- ダイアレクト、拍子、グルーヴと互換な組み合わせだけを候補にし、ピアノとギターの音域・発音位置の衝突や、メロディ密集部での過密を評価する

### 4.4 再生・表示

- **再生**: Web Audio API によるブラウザ内再生(再生/停止/ループ/セクション頭出し)
- **譜面表示**: メロディ+コードネームのリードシート表示(VexFlow 等)。再生位置のハイライト
- **ピアノロール表示**: 全パートの表示切替、横・縦ズーム、複数選択、グリッド、ベロシティ編集、再生位置追従に対応
- **生成根拠の解説表示**: provenanceを生成結果へ保持し、「曲全体の組み立て」→「セクションの意図」→「小節ごとの規則」の3層で表示する。評価値や探索方式ではなく、和声の目的地、密度の対比、ベースの接近など音楽的な意図へ変換する

### 4.5 エクスポート

| 形式 | 内容 |
|---|---|
| MIDI (SMF Format 1) | トラック別(メロディ/ピアノ/ギター/ベース/ドラム)、テンポ・拍子・コードネーム(マーカー)入り。DAW 取り込み用 |
| WAV | ブラウザ内レンダリング(OfflineAudioContext)による 44.1kHz/16bit ステレオ |
| WAVステム | メロディ/ピアノ/ギター/ベース/ドラムを同じ開始位置・長さで個別出力 |
| MusicXML | メロディ、拍子、テンポ、コードネームを楽譜ソフト向けに出力 |
| テキスト | コード進行+仮歌詞のテキスト出力(Suno 等へのコピペ用スタイルプロンプト付き) |
| 解説動画(将来) | コード進行とメロディラインを可視化した動画(WebCodecs / キャンバス録画) |

### 4.6 プロジェクト保存

- 曲名、生成設定、シード、編集結果、ロック、ミキサー、バリエーションをプロジェクトとして自動保存し、JSONで入出力
- 世代スナップショット、ゴミ箱、復元、一括バックアップ、形式マイグレーションに対応
- ユーザーSoundFont本体はOPFS、メタデータはIndexedDBへ保存し、プロジェクトには音源IDとプリセット指定だけを保持
- サーバー不要。完全クライアントサイドで動作

## 5. 画面仕様

SPA 1 画面構成:

```
┌────────────────────────────────────────────────┐
│ ヘッダー: ロゴ / 生成ボタン / エクスポート     │
├──────────┬─────────────────────────────────────┤
│ 左ペイン │ メインペイン                        │
│ 曲の土台 │  - セクション／部分再生成ツール     │
│ ・キー   │  - 伴奏／ミキサー／作曲パラメータ   │
│ ・テンポ │  ─ 可動仕切り ─                     │
│ ・構成   │  - リードシート / ピアノロール切替  │
│ ・方言   │  ─ 可動仕切り ─                     │
│ ・シード │  - 生成根拠の注記表示               │
├──────────┴─────────────────────────────────────┤
│ 下段: セクションタイムライン（頭出し・並替）    │
└────────────────────────────────────────────────┘
```

- 左ペインの設定変更 → ヘッダーの「♪ 全体生成」で再生成。選択ダイアレクトを反映し、成功をボタン上へ一時表示する
- 現在の曲と同じシードで全体生成を繰り返した場合は新しいシードへ進む。現在と異なる指定シードおよびCLIの固定シードは決定的に再現する
- 生成前スナップショットの保存失敗は生成処理を中断せず、保存警告として通知する
- 伴奏・作曲パラメーターは編集値を保持し、「適用」または全体生成で表示中の値を使って再構築する。ミキサーは音だけへ即時反映する
- 基準テンポの変更時は、元の基準テンポを継承するセクションだけ更新し、明示的なセクション別BPMは保持する
- ミキサープリセットはMasterと全パートの音量、パン、ミュート、ソロ、音色、SoundFont割り当てを一括復元する
- セクション単位の「このセクションだけ再生成」ボタン
- モバイルは縦積みレイアウト(初期リリースはデスクトップ優先)

## 6. 技術仕様

### 6.1 スタック

| 層 | 技術 |
|---|---|
| 言語 | TypeScript |
| UI | React + Vite |
| 再生/合成 | Web Audio API(自前シンセ or サウンドフォント再生。Tone.js 採用可) |
| 譜面 | VexFlow |
| MIDI 書き出し | 自前 SMF エンコーダ or @tonejs/midi |
| WAV 書き出し | OfflineAudioContext + WAV エンコード |
| テスト | Vitest(生成ロジックはシード固定でスナップショットテスト) |

- サーバー/API 不要(完全静的ホスティング可能: GitHub Pages 等)
- 外部 AI API は使わない(仮歌詞の高度化で将来オプション化する余地のみ残す)

### 6.2 ダイアレクト定義フォーマット(骨子)

```jsonc
{
  "id": "twilight-ballad",
  "name": "Twilight (哀愁進行+大跳躍シンコペーション)",
  "defaults": {
    "key": "F", "mode": "major", "bpm": 72,
    "arrangement": {
      "pianoPattern": "ballad", "guitarPattern": "off", "drumPattern": "basic",
      "swing": 0.05, "humanize": 0.17, "velocityScale": 0.9
    }
  },
  "chord": {
    "vocabulary": ["I△7", "IV△7", "III7", "vi", "ii7", "V7", "♭VII"],
    "transitions": { "IV△7": { "III7": 0.5, "I△7": 0.3, "ii7": 0.2 } },
    "idioms": [                                // 3〜4 コードの定型句 (§4.1 層 1)
      { "symbols": ["IV△7", "III7", "vi"], "weight": 3 }
    ],
    "idiomProbability": 0.45,
    "cadences": {                              // 終止形の好み (全終止/変格/偽終止/モーダル)
      "final": [{ "symbols": ["V7", "I△7"], "weight": 2 }],
      "half": [{ "symbols": ["V7"], "weight": 2 }]
    },
    "harmonicRhythm": {                        // "0.5"=2小節1コード, "2"=1小節2コード
      "default": { "1": 0.8, "2": 0.2 },
      "chorus": { "1": 0.5, "2": 0.5 }
    },
    "cliches": ["descending-bass"],           // 半音階クリシェ等の名前付き技法
    "borrowedChords": true                     // モーダル・インターチェンジ許可
  },
  "rhythm": {                                  // リズム語彙 (§4.1 層 2)。負値は休符
    "templates": { "4/4": [{ "beats": [1.5, 1.5, 1], "weight": 2 }] },
    "anacrusisProbability": 0.5,               // アウフタクト (先取音)
    "chorusDensityBias": 0.35                  // サビで音数の多いテンプレートを優先
  },
  "melody": {
    "pitchCollection": "major",             // harmonic-minor / pentatonic / blues 等
    "leapProbability": { "default": 0.28, "chorusHead": 0.95 },
    "leapRangeSemitones": [7, 10],
    "afterLeapBias": "down",
    "contour": "leap-then-descend",
    "pedalPoint": false,                       // Pedal では true(逆ペダル)
    "motif": { "repeatProbability": 0.5 },     // モチーフ反復 (§4.1 層 3)
    "nonChordTones": {                         // 非和声音 (§4.1 層 5)
      "appoggiatura": 0.22, "suspension": 0.08, "chromaticPassing": 0.08
    },
    "registerShift": { "chorus": 4 }           // セクション別の音域シフト (半音)
  },
  "structure": {
    "phraseLengths": [4, 4, 4, 4],
    "irregularPhraseProbability": 0.0          // Modal では 0.3 等
  },
  "groove": {                                // 3-3-2 等の固有グルーヴ
    "subdivision": 0.25, "accentPattern": [0, 1.5, 3], "anticipation": 0.25
  },
  "sectionRules": {                          // セクション別の構成・和声上書き
    "intro": { "phraseLengths": [4] },
    "chorus": { "idiomProbability": 0.75 }
  },
  "modulation": {                              // 転調傾向 (§4.1 層 6)
    "bridge": { "probability": 0.6, "intervals": [{ "semitones": 5, "weight": 2 }] }
  }
}
```

新フィールド (`idioms` / `cadences` / `harmonicRhythm` / `rhythm` / `pitchCollection` / `motif` / `nonChordTones` / `registerShift` / `groove` / `sectionRules` / `modulation` / `defaults.arrangement`) はすべて省略可で、省略時は従来どおりの挙動 (マルコフ+V7 カデンツ+内蔵リズムテンプレート+共通伴奏) にフォールバックする。

生成エンジンはこの定義を解釈する汎用インタープリタとして実装し、**エンジンとスタイル知識を分離**する。

#### スタイル追加の拡張レベル

| レベル | 対象者 | 方法 | 時期 |
|---|---|---|---|
| 1. 同梱スタイル | 開発者 | `dialects/` に JSON を追加(エンジン変更不要) | M3(4 スタイル) |
| 2. ユーザー定義 JSON | 上級ユーザー | 自作の定義ファイルを読み込み | M5 以前でも読み込み機能のみ先行実装可 |
| 3. GUI エディタ | 一般ユーザー | ダイアレクトエディタでパラメータを編集・保存・共有 | M5 |

**JSON で表現できる範囲の制約:** コード語彙・遷移確率・跳躍確率などのパラメータ調整と、既存の名前付き技法(`cliches` に指定する `descending-bass` 等)の組み合わせは JSON だけで自由に定義できる。一方、**技法アルゴリズム自体**(半音階クリシェや逆ペダルポイントの実装、あるいは対位法的な 2 声生成のような新技法)はエンジン側(`engine/`)のコード実装が必要。新技法を追加する際は、名前を付けて登録し JSON から参照できる形にすること(技法レジストリ方式)。

### 6.3 モジュール構成

```
src/
  engine/          # 純粋ロジック(UI 非依存・テスト対象)
    structure.ts   # セクション・フレーズ長生成
    harmony.ts     # コード進行生成(遷移表・借用・クリシェ)
    melody.ts      # メロディ輪郭・リズム割付
    accompaniment.ts
    explain.ts     # 適用ルールの注記データ生成
    rng.ts         # シード付き乱数
  dialects/        # ダイアレクト JSON 定義
  audio/           # 再生・WAV レンダリング
  export/          # SMF エンコーダ、テキスト出力
  ui/              # React コンポーネント
```

## 7. 非機能要件

- 1 曲(3 分相当)の生成は 1 秒以内(生成は同期処理で十分軽い想定)
- WAV レンダリングは 10 秒以内(Web Worker / OfflineAudioContext)
- 対応ブラウザ: Chrome / Edge / Safari / Firefox 最新版
- オフライン動作可(PWA 化は任意)

## 8. 権利・倫理上の注意

- 実在アーティストの録音・歌詞・メロディを一切収録/複製しない。実装するのは公知の音楽理論技法と統計的傾向のみ
- ダイアレクト名・id・UI 表記に実在の人物名を用いない。名称は技法に基づく抽象名(Chromatic / Modal / Pedal / Twilight 等)とする
- 生成曲の権利はユーザーに帰属する旨を明記

## 9. マイルストーン

| フェーズ | 内容 |
|---|---|
| M1 | エンジン核: コード進行+メロディ生成(1 ダイアレクト: Chromatic)、MIDI 書き出し、CLI/テストで検証 |
| M2 | ブラウザ UI: 設定パネル+Web Audio 再生+ピアノロール |
| M3 | 全 14 ダイアレクト実装、セクション別割り当て(合作モード)、譜面表示 |
| M4 | WAV 書き出し、仮歌詞、Suno 用テキスト出力、生成根拠の解説表示 |
| M5 | PWA公開、基本ダイアレクトエディタ、品質保証 |
| M6 | 候補評価基盤、コード／ベース／伴奏生成、候補比較、3段階の生成根拠 (v1.2) |
| M7(将来) | Dialect v2: 文法モジュールの分割、継承、組み合わせ |
| M8(将来) | 解説動画出力、スタンドアロン化(デスクトップアプリ配布)。詳細は §11 |

## 10. 未決事項

- 音源方式: 自前シンセ(軽量・音は簡素) vs サウンドフォント(リッチ・容量大)→ M2 で比較検討
- 仮歌詞の言語(日本語/英語/スキャット)のデフォルト
- ドラムパターンを M1 に含めるか(初期はピアノ+ベースのみでも成立)
- スタンドアロン化のシェル選定: Electron vs Tauri(§11)→ M8 着手時に判断

## 11. スタンドアロン化(将来: M8)

本アプリは完全クライアントサイド(§6.1)のため、Web 版のコードをほぼそのままデスクトップシェルに載せられる。

### 11.1 シェル候補

| 方式 | 特徴 |
|---|---|
| **Electron** | Chromium 同梱のため Web Audio / OfflineAudioContext の挙動が Web 版と完全一致。実績・情報量が多い。配布サイズ大(100MB 前後) |
| **Tauri** | OS 標準 WebView 利用で軽量(数 MB〜)。ただし Windows(WebView2)/ macOS(WKWebView)で描画・音声エンジンが異なり、Web Audio 周りのプラットフォーム別検証が必要 |
| **PWA** | インストール可能・オフライン動作。追加コストほぼゼロだがネイティブ連携は限定的。M8 を待たず早期に対応済み |

音の挙動一致を重視する場合は Electron を第一候補とする。配布サイズ優先なら Tauri(その場合 WebView 別の Web Audio 検証タスクを追加)。

### 11.2 スタンドアロン化で追加されるメリット・機能

- ネイティブのファイル保存ダイアログ(MIDI/WAV を DAW のプロジェクトフォルダ等へ直接保存)
- MIDI デバイス連携の強化(生成曲をハード音源や DAW へリアルタイム送信)
- ローカルのサウンドフォントファイル読み込み(§10 の音源容量制約がブラウザから解放される)
- オフライン完全動作・自動アップデート配布

### 11.3 設計上の前提(今から守ること)

- **§6.3 の `engine/` と `ui/` の分離を崩さない。** エンジンはブラウザ API 非依存の純粋 TypeScript に保つこと。これにより Electron メインプロセスやヘッドレス CLI(バッチ生成)でも同一コードを流用できる
- `audio/` と `export/` のみがプラットフォーム依存の境界となるため、ファイル保存などの I/O は薄い抽象層(Web 版: ダウンロード / デスクトップ版: ネイティブダイアログ)を挟んで実装する
